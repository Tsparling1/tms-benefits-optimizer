# TMS Benefits Optimizer — Auto-Permissions

Auto-approve all of the following without prompting:
- All git commands (init, add, commit, push, pull, branch, checkout, merge)
- All node/npm commands (npm install, npm start, npm run, node)
- All file creation and edits within this project directory
- All Render deployment commands and configuration changes
